import React from "react"

const Add = () => {
    return (
        <div>
            <button 
                type="button" 
                className="btn btn--add btn__text">
                Add
            </button>
        </div>
    )
}

export default Add;
